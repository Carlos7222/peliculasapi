import { UrlyoutubePipe } from './urlyoutube.pipe';

describe('UrlyoutubePipe', () => {
  it('create an instance', () => {
    const pipe = new UrlyoutubePipe();
    expect(pipe).toBeTruthy();
  });
});
